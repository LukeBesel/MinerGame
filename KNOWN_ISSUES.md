# Known Issues

Updated every phase. See also docs/DECISIONS.md for intentional scope calls.

- Phase 0: none blocking. `--check-only` cannot resolve autoload identifiers (documented
  workaround in ARCHITECTURE.md §2); global class_name cache unavailable headless (worked
  around — no class_name in codebase).
